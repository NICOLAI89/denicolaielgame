# Project Settings Placeholder

Create the Unity project through Unity Hub, then let Unity generate the real `ProjectSettings` files. This repository intentionally avoids committing a guessed Unity editor version in Iteration 10.

